"""NL2SQL backend aplikacija.

Glavni Python paket — sadrži sve module backend servisa: API routere,
LLM providere, validation pipeline, services orkestraciju, evaluation
runner i database layer.

Entry point: ``app.main:app`` (FastAPI instanca).
"""

__version__ = "0.1.0"
